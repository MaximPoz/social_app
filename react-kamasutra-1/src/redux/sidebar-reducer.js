     let initialState = {
        friendsOnline: [
            { id: 1, name: 'Lena' },
            { id: 2, name: 'Viktor' },
            { id: 3, name: 'Artur' }
        ],
    }

    const sidebarReducer = (state = initialState, action) => {

    return state;
}


export default sidebarReducer;